namespace Allergies.Models
{
  public class someFunction {
        int [] tempList;
        for (int i =0; i<validScore.length; i++) {
        if (enteredScore > 255) {
            print an error message;
        }
        else {
            if (validScore[i] == enteredScore) {
            go to dictionary and find value corresponding to key "validScore[i]" and push single element to a list;
            }
            else (validScore[i] < enteredScore) {
                iterate thru validScore and find all entries less than enteredScore push each element to a list;
            }
            outputList = AllergyFunction(list)
            for each element in outpuList print out the values.
        }
    }
}